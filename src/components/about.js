import React, { Component } from "react";

export default class About extends Component {
    render() {
        return (
            <div style={{fontSize: "20px"}}>О компании</div>
        );
    }
}